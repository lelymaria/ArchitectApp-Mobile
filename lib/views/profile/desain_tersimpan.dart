import 'package:flutter/material.dart';

class DesainTersimpan extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("Desain Tersimpan"))
      ),
    );
  }
}